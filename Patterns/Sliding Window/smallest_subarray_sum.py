# Given an array of integers and a positive number (s), find the length
# of the smallest contiguous subarray greater than or equal to
# (s). 
# return 0 is there isn't any

def smallest_subarray_sum(s, array):
    return